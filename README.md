# Копейка («Расчетки»)

Программа сотрудника АО «АВТОВАЗ»:  
Приложение для сбора расчеток из почты: находит письма с расчетками,
скачивает PDF, показывает историю начислений. Windows + Android (Kivy/KivyMD).

## Структура
- main.py — интерфейс и логика
- pdf_parser.py — разбор PDF-расчеток
- storage.py — настройки и база (ПК: папка пользователя; Android: папка приложения)
- main.spec — сборка exe (PyInstaller), имя сборки Kopeyka
- buildozer.spec — сборка APK
- .github/workflows/android.yml — автосборка APK (GitHub Actions)
- ИНСТРУКЦИЯ.md — руководство пользователя

## Сборка Windows
pip install -r requirements.txt
pyinstaller main.spec
Результат: dist/Kopeyka/ (Kopeyka.exe + _internal) → запаковать в zip.

## Сборка Android
сборка запускается вручную: «Actions → Run workflow» для веток main / android-test.
Подпись: секрет DEBUG_KEYSTORE_B64. НЕ перегенерировать ключ —
иначе обновления перестанут ставиться поверх.

## Возможности
- Несколько почтовых ящиков, автопроверка 1–5 числа
- Снятие пароля PDF и разбор: получка, начисления, удержания, коды
- Справочник 128 кодов АВТОВАЗ с поиском и карточкой кода
- «О программе», проверка обновлений с GitHub.
- Пароли хранятся локально и наружу не передаются


## Известные особенности
- NVIDIA: если окно мерцает при перетаскивании — в панели NVIDIA
  оставить G-SYNC только для полноэкранного режима (убрать «и в окне»).

## История версий
## История версий
Подробности — на странице [Releases](https://github.com/Hirdmen/Kopeyka/releases).
- **1.0.3** — инструкция по настройке с кликабельными ссылками, всплывающие плитки кодов, обновления поверх без удалений, прорисовка после фона.
- **1.0.2** — пакет «Копейка», разбор PDF с паролем, справочник кодов, exe сразу Kopeyka.exe.
- **1.0.1** — первый публичный релиз: «О программе», обновления, донат, справочник.
- **1.0** — почта, парсинг, база, автопроверка 1–5 числа.

## Запуск на ПК
1. Python 3.10+
2. `pip install kivy kivymd pypdf`
3. `python main.py` (или `pythonw main.py` — без консоли)

